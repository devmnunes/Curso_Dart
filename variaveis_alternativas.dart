void main() {
  var nome = 'Matheus'; // Var permite você atribuir um valor a variavel sem saber o tipo primitivo dela, porém assim que atribuida um valor a variavel não podera mudar a tipo primitivo dela.
  
  dynamic idade; //Variavel dynamic permite alterar o tipo da variavel ex: de Int para String.
  idade = 27;
  print(idade);
  print(nome);
}
